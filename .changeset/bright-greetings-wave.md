---
"@lynx-example/custom-framework": minor
---

Add a custom framework example that compiles `Greeting.lynx` into dual-thread Lynx and Web bundles, including binary and legacy JSON Web template compatibility.
