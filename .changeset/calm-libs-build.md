---
"@lynx-example/external-bundle": patch
---

Add a background-only utils external bundle, build the plain JavaScript lodash bundle without depending on `pluginReactLynx`, and document the supported external bundle scenarios.
