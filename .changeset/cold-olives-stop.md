---
"@lynx-example/design-guide": patch
---

Fix `force_field` example pointer coord drift when NavBar present by aligning clientX/clientY with screen-based boundingClientRect coords (not original web semantics).
