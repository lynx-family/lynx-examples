---
"@lynx-example/css-preprocessors": minor
"@lynx-example/css-modules": minor
"@lynx-example/css-global": minor
"@lynx-example/css-postcss": minor
---

Add four styling examples that build the same info card through different pipelines: `css-global` (plain stylesheet), `css-modules` (locally scoped `*.module.css`), `css-preprocessors` (Sass, Less and Stylus) and `css-postcss` (`px` authored against a 375px design width, shipped as `vw`).
