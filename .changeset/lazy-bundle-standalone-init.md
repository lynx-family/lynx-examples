---
"@lynx-example/lazy-bundle-standalone": patch
---

Add the `lazy-bundle-standalone` example, which renders the same page as `lazy-bundle` but builds the lazy bundle standalone in a separate producer build and loads it by URL.
