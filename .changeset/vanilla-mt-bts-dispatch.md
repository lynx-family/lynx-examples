---
"@lynx-example/vanilla": patch
---

Fix main-thread → background event dispatch in the vanilla common helpers: dispatching on `lynx.getCoreContext()` from the main thread is a self-loop and never reaches the background thread; use `lynx.getJSContext()` instead. Also fix duplicate todo ids generated by `addTodo` in the todolist example.
