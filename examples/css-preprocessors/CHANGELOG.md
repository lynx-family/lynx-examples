# @lynx-example/css-preprocessors

## 0.1.1

### Patch Changes

- 8bbdca3: Upgrade shared Lynx and Rspeedy build dependencies.

## 0.1.0

### Minor Changes

- 4a9da12: Add four styling examples that build the same info card through different pipelines: `css-global` (plain stylesheet), `css-modules` (locally scoped `*.module.css`), `css-preprocessors` (Sass, Less and Stylus) and `css-postcss` (`px` authored against a 375px design width, shipped as `vw`).
